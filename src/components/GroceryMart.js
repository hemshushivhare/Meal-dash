
const GroceryMart = ()=>{
    return (
    <>
    <h1>This is our Grocery Mart </h1>
    <h3> It is build using chunking or say code splitting or dynamic Bundling,lazy loading , on demand loading,dynamix import</h3>
    <p> This grocery mart will have its own large no. of child components so we do code splitting in order to optimize the performance</p>

    </>


    );
}

export default GroceryMart;